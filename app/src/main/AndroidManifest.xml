<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.antonius.homy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".KontrolPenyediaActivity"
            android:label="@string/title_activity_kontrol_penyedia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".KatalogActivity" />
        <activity android:name=".CustomerMain" />
        <activity android:name=".search" />
        <activity android:name=".SearchPage" />
        <activity android:name=".CustomerTransaksi"></activity>
    </application>

</manifest>